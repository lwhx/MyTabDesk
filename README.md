# MyTabDesk

MyTabDesk 是一个本地优先的浏览器新标签页工作台插件，用于保存、整理和恢复浏览器标签页。它会替换浏览器默认新标签页，并通过“空间 - 分组 - 链接”的结构帮助用户管理不同任务场景下的网页。

## 核心功能

- 三栏新标签页工作台
- 空间创建、切换、删除和排序
- 分组创建、折叠、删除和排序
- 链接展示、打开、删除和跨分组移动
- 当前窗口标签页读取与保存
- 当前空间链接搜索
- 深色模式、左栏折叠、右栏折叠
- 批量删除链接
- JSON 数据导入和导出
- 加密备份导入和导出
- 手动同步能力规划

## 安装方式

1. 打开 `chrome://extensions/` 或 `edge://extensions/`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择当前项目目录。
5. 打开新标签页即可使用 MyTabDesk。

## 使用说明

1. 在左侧创建或切换工作空间。
2. 在中间区域创建分组，或从右侧保存当前窗口标签页。
3. 点击链接卡片可在新标签页打开网页。
4. 点击分组的“打开全部”可恢复该分组内保存的网页。
5. 使用搜索框按分组名、链接标题或 URL 搜索。
6. 在设置页中可以导入、导出和加密备份数据。

## 权限说明

当前扩展使用以下权限：

| 权限 | 用途 |
|---|---|
| `tabs` | 读取当前窗口标签页、切换标签页、打开新标签页 |
| `storage` | 将空间、分组、链接和设置保存到本地浏览器 |

项目默认不读取浏览历史、收藏夹、Cookie，也不会将数据上传到服务器。

## 数据与隐私

所有工作台数据默认保存在 `chrome.storage.local` 中。普通导出文件和加密备份文件由用户手动下载和保存。请妥善保管备份文件和加密密码。

如果启用后续同步功能，远程地址、账号或访问令牌只应保存在本地浏览器扩展存储中，不应写入代码仓库或公开文档。

## 开发与测试

项目不依赖构建工具，可以直接作为浏览器扩展加载。

运行核心逻辑测试：

```powershell
npm test
```

等价命令：

```powershell
node tests/tabdesk-core.test.js
```

## 项目结构

```text
MyTabDesk/
├── assets/
├── tests/
│   └── tabdesk-core.test.js
├── manifest.json
├── newtab.html
├── newtab.css
├── newtab.js
├── tabdesk-core.js
└── README.md
```

## 后续计划

- 标准化普通备份格式
- 增强右侧当前标签页搜索和单个保存能力
- 使用 AES-GCM 升级加密备份
- 增加 WebDAV / GitHub Gist 手动同步
- 持续补充测试覆盖
